import {ChevronLeft,ChevronRight,Pause,Play,RotateCcw} from 'lucide-react'
import {NetworkSelector} from './NetworkSelector'
import type {Network} from '../../types/scenes'
export function AnimationControls({playing,onPrevious,onNext,onToggle,onRestart,network,onNetwork,speed,onSpeed}:{playing:boolean;onPrevious:()=>void;onNext:()=>void;onToggle:()=>void;onRestart:()=>void;network:Network;onNetwork:(n:Network)=>void;speed:number;onSpeed:(n:number)=>void}){return <div className="control-row"><div><button onClick={onPrevious}><ChevronLeft/> PREVIOUS</button><button className="play" onClick={onToggle}>{playing?<Pause/>:<Play/>}{playing?'PAUSE':'PLAY FILM'}</button><button onClick={onNext}>NEXT <ChevronRight/></button><button onClick={onRestart}><RotateCcw/> RESTART</button></div><div className="speed-control"><small>SPEED</small>{[.5,1,1.5,2].map(value=><button className={speed===value?'active':''} onClick={()=>onSpeed(value)} key={value}>{value}×</button>)}</div><NetworkSelector network={network} onChange={onNetwork}/></div>}
