import {ContractStorage} from './ContractStorage'
export function ContractAccount({publicNet,count,onInspect}:{publicNet:boolean;count:number;onInspect:(id:string)=>void}){const id=publicNet?'publicContract':'contract';return <div data-component={id} className="contract" onClick={()=>onInspect(id)} role="button" tabIndex={0}><b>{publicNet?'SEPOLIA':'COUNTER'} CONTRACT ACCOUNT</b><button data-component="address" onClick={e=>{e.stopPropagation();onInspect('address')}}>{publicNet?'0x742…':'0x5FbDB231…'}</button><code>CODE 0x60806040…</code><ContractStorage count={count} onInspect={onInspect} id={publicNet?'publicStorage':'storage'}/></div>}
