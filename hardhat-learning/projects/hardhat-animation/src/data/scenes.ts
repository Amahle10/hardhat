export type Scene={label:string;kicker:string;title:string;description:string;input:string;process:string;output:string;focus:string;packet?:string}
export const scenes:Scene[]=[
 {label:'Source',kicker:'01 / SOURCE',title:'Solidity is only source code',description:'Counter.sol defines intent. Nothing has been deployed: there is no address and no blockchain state yet.',input:'Counter.sol',process:'Developer writes contract logic',output:'Source file — not a contract account',focus:'source'},
 {label:'Hardhat',kicker:'02 / ORCHESTRATION',title:'Hardhat starts the workflow',description:'The command starts Hardhat, which discovers the project and loads its configuration.',input:'npx hardhat build',process:'Initialize development environment',output:'Configured project workflow',focus:'hardhat',packet:'COMMAND'},
 {label:'Config',kicker:'03 / CONFIGURATION',title:'Project behavior is loaded',description:'Compiler version, network endpoints, plugins and signing accounts flow from hardhat.config.ts into Hardhat.',input:'hardhat.config.ts',process:'Resolve compiler + network settings',output:'Solidity 0.8.28 · Sepolia · signer',focus:'config',packet:'CONFIG'},
 {label:'Compile',kicker:'04 / COMPILATION',title:'Solidity becomes Ethereum artifacts',description:'solc transforms source into an ABI for interaction and bytecode the EVM can execute.',input:'Counter.sol',process:'Solidity compiler (solc)',output:'ABI + bytecode in artifacts/',focus:'compiler',packet:'COMPILE'},
 {label:'Local Node',kicker:'05 / LOCAL RUNTIME',title:'A separate Ethereum simulation boots',description:'Hardhat controls a local node, but Hardhat is not the blockchain. The node exposes JSON-RPC, accounts, blocks, an EVM and state.',input:'npx hardhat node',process:'Start local Ethereum runtime',output:'127.0.0.1:8545 · Block #0',focus:'local',packet:'BOOT'},
 {label:'Deploy',kicker:'06 / DEPLOYMENT SCRIPT',title:'The script uses compiled artifacts',description:'deploy.ts asks ethers for Counter. ContractFactory retrieves its ABI and bytecode — not Counter.sol.',input:'deploy.ts + “Counter”',process:'ethers → ContractFactory → artifacts',output:'Deployable contract factory',focus:'deploy',packet:'ARTIFACTS'},
 {label:'Transaction',kicker:'07 / CREATION TRANSACTION',title:'A contract-creation transaction is built',description:'The transaction contains creation bytecode. Its destination is null because the contract address does not exist yet.',input:'ABI + creation bytecode',process:'Signer builds and signs transaction',output:'from 0xf39F… · to null · data 0x6080…',focus:'tx',packet:'TX'},
 {label:'RPC',kicker:'08 / JSON-RPC',title:'The transaction travels to the node',description:'JSON-RPC is the interface between Ethereum software and a node. eth_sendRawTransaction carries signed bytes into the local runtime.',input:'Signed transaction',process:'ethers → provider → JSON-RPC',output:'Transaction received by local node',focus:'rpc',packet:'RPC'},
 {label:'EVM',kicker:'09 / EVM EXECUTION',title:'A contract account comes into existence',description:'The EVM executes creation code. Runtime bytecode and initial storage are installed under a newly derived contract address.',input:'Deployment transaction',process:'Pool → Block #1 → EVM',output:'Contract account · code · count = 0',focus:'evm',packet:'TX'},
 {label:'Address',kicker:'10 / RECEIPT',title:'The new address returns to the script',description:'A successful receipt travels back through JSON-RPC. The address identifies the contract account inside Ethereum state.',input:'Execution result',process:'Node → provider → ethers → deploy.ts',output:'0x5FbDB231…',focus:'contract',packet:'RECEIPT'},
 {label:'Function Call',kicker:'11 / WRITE CALL',title:'increment() becomes a new transaction',description:'The ABI encoder converts the function into calldata. Unlike deployment, this transaction targets the contract address.',input:'increment()',process:'ABI encode → 0xd09de08a → sign',output:'to 0x5Fb… · data 0xd09de08a',focus:'call',packet:'CALL'},
 {label:'State',kicker:'12 / STATE TRANSITION',title:'EVM execution changes storage',description:'The write transaction joins Block #2. The EVM runs Counter.increment() and persists count from 0 to 1.',input:'Function transaction',process:'Block #2 → EVM → SSTORE',output:'Blockchain state updated: 0 → 1',focus:'storage',packet:'TX'},
 {label:'Sepolia',kicker:'13 / NETWORK SWITCH',title:'The target changes to public Sepolia',description:'Hardhat remains on your computer. Configuration selects a remote RPC endpoint and a real testnet signing account.',input:'--network sepolia',process:'Load Sepolia RPC URL + account',output:'Remote deployment route',focus:'sepolia',packet:'CONFIG'},
 {label:'Public RPC',kicker:'14 / REMOTE JSON-RPC',title:'A signed transaction crosses the internet',description:'The RPC provider is not the blockchain; it provides access to an Ethereum node connected to Sepolia.',input:'Signed transaction',process:'Provider → RPC service → Ethereum node',output:'Transaction propagated to Sepolia',focus:'remoteRpc',packet:'SIGNED TX'},
 {label:'Public Deploy',kicker:'15 / SEPOLIA LIFECYCLE',title:'Sepolia validators include the deployment',description:'The public transaction moves through a node pool, validator, block and EVM before a contract appears in shared testnet state.',input:'Propagated deployment transaction',process:'Pool → validator → block → EVM',output:'Sepolia contract · count = 0',focus:'publicContract',packet:'TX'},
 {label:'Public State',kicker:'16 / PUBLIC RECEIPT',title:'The public address returns',description:'A receipt returns through the node and RPC provider. The contract is now queryable by anyone connected to Sepolia.',input:'Sepolia execution result',process:'Sepolia → RPC → ethers → Hardhat',output:'Contract address 0x742…',focus:'publicContract',packet:'RECEIPT'},
 {label:'Increment',kicker:'17 / PUBLIC STATE',title:'A public state transition completes the flow',description:'increment() is signed, propagated, validated, executed and recorded. Sepolia’s shared contract storage becomes count = 1.',input:'counter.increment()',process:'RPC → validator → block → EVM',output:'PUBLIC TESTNET STATE UPDATED',focus:'publicStorage',packet:'CALL'},
]
